name: gitleaks
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions: read-all

jobs:
  gitleaks:
    permissions:
      pull-requests: write
    uses: fish-shop/workflows/.github/workflows/gitleaks.yml@9063758be54c714754072cbc1e8c57ad6544d7e4 # v1.12.1
    secrets: inherit
