name: Gitleaks SAST scan
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions: read-all

jobs:
  gitleaks:
    name: Gitleaks SAST scan
    permissions:
      pull-requests: write
    uses: fish-shop/workflows/.github/workflows/gitleaks.yml@a20601bed8121b1eeee854f77745b5737e208f7e # v1.13.4
    secrets:
      gitleaks_license: ${secrets.GITLEAKS_LICENSE}
