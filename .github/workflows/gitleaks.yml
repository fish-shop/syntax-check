name: Gitleaks SAST scan
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions: read-all

jobs:
  gitleaks:
    name: Gitleaks SAST scan
    permissions:
      pull-requests: write
    uses: fish-shop/workflows/.github/workflows/gitleaks.yml@01b9ba53787be7d3c655e7409c5640bad274ea3c # v1.13.34
    secrets:
      gitleaks_license: ${secrets.GITLEAKS_LICENSE}
