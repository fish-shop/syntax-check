name: Gitleaks SAST scan
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions: read-all

jobs:
  gitleaks:
    name: Gitleaks SAST scan
    permissions:
      pull-requests: write
    uses: fish-shop/workflows/.github/workflows/gitleaks.yml@839b374bc00ff9bd323fe60a3fda90b6bed823c7 # v1.13.6
    secrets:
      gitleaks_license: ${secrets.GITLEAKS_LICENSE}
